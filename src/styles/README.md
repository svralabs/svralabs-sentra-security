# Style Guide and Theme Documentation

## Theme System

This project uses a CSS variable-based theming system with Tailwind CSS. The theme is defined in `src/styles/theme.js` and applied in `src/styles/globals.css`.

### Light Theme
